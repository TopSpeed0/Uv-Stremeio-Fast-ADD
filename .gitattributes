# Shell scripts must keep LF or sh chokes on the carriage returns
*.sh text eol=lf

# PowerShell is happiest with CRLF on Windows
*.ps1 text eol=crlf

# Keep the addon profile stable across platforms
*.json text eol=lf
